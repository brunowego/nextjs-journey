{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@acme/db",
  "private": true,
  "main": "./index.ts",
  "scripts": {
    "db:seed": "npx convex import --table products ./seed/products.jsonl",
    "dev": "convex dev",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "@auth/core": "~0.34.2",
    "convex": "~1.14.1"
  },
  "devDependencies": {
    "@acme/typescript-config": "workspace:~",
    "@types/node": "~22.4.1"
  }
}
