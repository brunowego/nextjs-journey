{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "acme",
  "private": true,
  "scripts": {
    "db:seed": "turbo db:seed",
    "db:dashboard": "turbo db:dashboard",
    "lint": "trunk check",
    "format": "trunk fmt",
    "dev": "turbo dev",
    "deploy": "turbo deploy"
  },
  "dependencies": {
    "turbo": "~2.0.14"
  },
  "devDependencies": {
    "@acme/typescript-config": "workspace:~",
    "@biomejs/biome": "~1.8.3",
    "@trunkio/launcher": "~1.3.1"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.7.1",
  "volta": {
    "node": "20.12.1"
  }
}
